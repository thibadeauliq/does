# does

A fast personal task runner — define repeatable "does" (mini-scripts) and run them from anywhere.

```
does deploy:staging
does db:seed
does test:unit
```

## Install
```bash
npm install -g does-runner
```
