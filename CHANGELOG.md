# Changelog

## 0.3.0
- Add `--parallel` flag for concurrent task execution
- Add `does --list` to enumerate available tasks

## 0.2.0
- Config discovery walks up directory tree
- Multi-step `run` arrays
- Per-task `env` overrides

## 0.1.0
- Initial release: basic task runner
